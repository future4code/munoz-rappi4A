import styled from "styled-components"

export const RestaurantMenu = styled.main`
    display: flex;
    flex-direction: column;
    gap: 8px;
    align-items: center;
    width: 100vw;
    min-height: 100vh;
    height: fit-content;
`